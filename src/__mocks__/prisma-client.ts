/**
 * Mock for Prisma Client in Jest (generated client uses ESM import.meta).
 */
export const PrismaClient = class {
  $connect = () => Promise.resolve();
  $disconnect = () => Promise.resolve();
};
